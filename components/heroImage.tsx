import Image from "next/image";

const HeroImage = (params) => {
  return (
    <div className="w-screen bg-center h-96 bg-no-repeat bg-cover  bg-hero-image "></div>
  );
};

export default HeroImage;
